//GENERATE FIBONACCI SERIES UPTO FIRST 10 TERMS

#include<stdio.h>
void main(){
    int a=0,b=1,c=0;
    printf("%d %d", a,b);
    for(int i=2;i<=10;i++)
    {
      c=a+b;
      printf("%d \t",c);
      a=b;
      b=c;
    }
}
